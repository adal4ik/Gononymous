package fileHandlers

// Init initializes the file handlers package
func Init() error {
	// Currently just ensures storage is initialized
	// Can add file-specific initialization here if needed
	return nil
}
